name: Temporary part3 disconnect fencing patch

on:
  push:
    branches:
      - fix/871-android-gatt-lifecycle-integration
    paths:
      - .github/workflows/_temporary_part3_patch2.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/871-android-gatt-lifecycle-integration
          fetch-depth: 0
      - name: Apply patch
        run: python3 .github/_temporary_part3_patch2.py
      - name: Commit patch
        run: |
          rm .github/_temporary_part3_patch2.py
          rm .github/workflows/_temporary_part3_patch2.yml
          git config user.name ODevStudio
          git config user.email ODevStudio@proton.me
          git add -A
          git diff --cached --check
          git commit -m 'fix(android): fence disconnect requests before teardown'
          git push origin HEAD:fix/871-android-gatt-lifecycle-integration
